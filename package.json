{
  "name": "nexode-mcp",
  "version": "0.1.3",
  "private": false,
  "type": "module",
  "description": "MCP tools for deploying apps on Nexode",
  "bin": { "nexode-mcp": "dist/index.js" },
  "files": ["dist", "skills", "README.md"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test test/*.test.mjs",
    "prepare": "npm run build",
    "prepack": "npm run build"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.1",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "typescript": "^5.7.3"
  }
}
